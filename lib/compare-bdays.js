// plan
    // getMonth() && getDate()
    // let closest = Math.min(...bdays)
    // new Date(closest).toDateString()
// what if
    // same day same month
// actually
    // break data into 4 : name & 3


// temporarily import data
// const rdf = require('./retrieve-data-file')
// const dataObjArr = rdf('./data.json')


// $()
const $ = _ => {  // objArr
// arr.reduce((a,b) => (a.n > b.n) ? a : b)
// just make the year fixed and then do it
// it all depents on returning all matched if

}


// testing
console.log($())